// TODO Replace with the actual type from Next.js once it is available.
/*

export type NextPageProps = {
  params: NextPageParams;
  searchParams: NextSearchParams;
};
 */

// TODO Replace with the actual type from Next.js once it is available.
export type NextPageParams = {
  slug: string;
};

// TODO Replace with the actual type from Next.js once it is available.
export type NextSearchParams = {
  search: string;
};
